Awesome! Let’s now build the full **Cart Page** where users can:

* ✅ View all added courses
* 🗑 Remove courses from cart
* ⭐ Move to wishlist
* 💵 See total & savings
* 📦 Place an order via checkout

---

## ✅ Step 9: Build `Cart.tsx`

---

### 🧱 1. **Update the file `src/pages/Cart.tsx`**

Paste this complete code:

```tsx
import { useCart } from '../context/CartContext';
import { useState } from 'react';
import { Course } from '../types/Course';

function Cart() {
  const { cart, removeFromCart, total } = useCart();
  const [wishlist, setWishlist] = useState<string[]>([]);
  const [orderPlaced, setOrderPlaced] = useState(false);

  const moveToWishlist = (course: Course) => {
    setWishlist(prev => [...prev, course.id]);
    removeFromCart(course.id);
    alert(`"${course.title}" moved to wishlist.`);
  };

  const handleCheckout = () => {
    setOrderPlaced(true);
  };

  const savings = cart.reduce((acc, course) => {
    return acc + (course.discountedPrice ? course.price - course.discountedPrice : 0);
  }, 0);

  if (orderPlaced) {
    return (
      <div>
        <h2>Order Placed ✅</h2>
        <p>Thank you for your purchase!</p>
      </div>
    );
  }

  return (
    <div>
      <h2>🛒 Your Cart</h2>
      {cart.length === 0 ? (
        <p>Your cart is empty.</p>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
          {cart.map(course => (
            <div
              key={course.id}
              style={{
                border: '1px solid #ccc',
                borderRadius: '8px',
                padding: '1rem',
                fontFamily: 'Montserrat, sans-serif',
              }}
            >
              <h4>{course.title}</h4>
              <p>Price: ₹{course.discountedPrice || course.price}</p>
              <div style={{ display: 'flex', gap: '1rem' }}>
                <button onClick={() => moveToWishlist(course)}>Move to Wishlist</button>
                <button onClick={() => removeFromCart(course.id)}>Delete</button>
              </div>
            </div>
          ))}

          <div
            style={{
              marginTop: '2rem',
              padding: '1rem',
              border: '2px solid #004aad',
              borderRadius: '10px',
              maxWidth: '400px',
              fontFamily: 'Montserrat, sans-serif',
            }}
          >
            <h3>Total: ₹{total}</h3>
            <p>Savings: ₹{savings}</p>
            <button onClick={handleCheckout} style={{ marginTop: '10px' }}>
              Checkout
            </button>
          </div>
        </div>
      )}
    </div>
  );
}

export default Cart;
```

---

### ✅ Now test in browser:

1. Go to `/cart`
2. You should see all courses you added
3. Try:

   * 🗑 Clicking **Delete** → removes course
   * ⭐ Clicking **Move to Wishlist** → removes from cart and shows alert
   * 🧾 Clicking **Checkout** → replaces screen with “Order Placed” message

---

## 🧾 What’s working now:

✅ Cart page with real-time updates
✅ Total + savings calculated
✅ Modal-like success state after checkout
✅ Works fully with your CartContext

---

✅ Let me know once this is working — and next we’ll build:

> ⭐ **WishlistContext** and the `Wishlist.tsx` page

Ready for that?
